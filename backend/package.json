{
  "name": "casper-agent-leash-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Backend for Casper Agent Leash — Gemini reasoning + on-chain enforcement via casper-client.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "@google/genai": "^2.10.0",
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^4.22.2"
  }
}
